package com.eap.lifepilot.custom_views;

import android.graphics.Matrix;

/**
 * InterfaceDecodeOnMatrixChangeListener is the interface to provide callbacks
 * to CustomImageViewForZoom.
 * 
 * @author Gateway Technolabs
 * 
 */
public interface InterfaceDecodeOnMatrixChangeListener {

	void onMatrixChangedCallBack(Matrix matrix);
}
